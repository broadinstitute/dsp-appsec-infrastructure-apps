apiVersion: v1
kind: PersistentVolume
metadata:
  name: ${SERVICE_VOLUME}
  namespace: ${NAMESPACE}
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 200Gi
  gcePersistentDisk:
    pdName: ${SERVICE_DISK}
