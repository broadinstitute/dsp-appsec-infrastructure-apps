apiVersion: dns.cnrm.cloud.google.com/v1beta1
kind: DNSRecordSet
metadata:
  name: ${NAMESPACE}
spec:
  name: ${DNS_HOSTNAME}.
  type: A
  ttl: 300
  managedZoneRef:
    name: ${DNS_ZONE}
  rrdatas:
  - ${IP_ADDRESS}
