apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ${INGRESS}
  namespace: ${NAMESPACE}
  annotations:
    kubernetes.io/ingress.allow-http: 'false'
    kubernetes.io/ingress.global-static-ip-name: ${IP_NAME}
    networking.gke.io/managed-certificates: ${MANAGED_CERT}
spec:
  rules:
  - host: ${DNS_HOSTNAME}
    http:
      paths:
      - path: /*
        backend:
          serviceName: ${SERVICE}
          servicePort: ${HTTP_PORT}
      - path: /ws/*
        backend:
          serviceName: ${SERVICE}
          servicePort: ${TCP_PORT}
---
apiVersion: cloud.google.com/v1beta1
kind: BackendConfig
metadata:
  name: ${BACKEND_CONFIG}
  namespace: ${NAMESPACE}
spec:
  securityPolicy:
    name: ${BROAD_INGRESS_CSP}
---
apiVersion: v1
kind: Service
metadata:
  name: ${SERVICE}
  namespace: ${NAMESPACE}
  annotations:
    cloud.google.com/neg: |
      {
        "ingress": true
      }
    beta.cloud.google.com/backend-config: |
      {
        "ports": {
          "${HTTP_PORT}": "${BACKEND_CONFIG}",
          "${TCP_PORT}": "${BACKEND_CONFIG}"
        }
      }
spec:
  type: NodePort
  selector:
    service: ${SERVICE}
  ports:
  - name: ${HTTP_PORT}
    port: 80
    targetPort: ${HTTP_PORT}
  - name: ${TCP_PORT}
    port: 7687
    targetPort: ${TCP_PORT}
